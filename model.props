global.random.seed = 139
stop.at = 20

# per functional group per cell 
cohort.count = 15
# per cell 
stock.count = 3;

# world definition props
# actually x and y ranges are set aat the momnet by SimulationControlparameters.csv
min.x = 0
min.y = 0
max.x = 35 
max.y = 12 
grid.buffer = 0

# these must multiply to total number of processes
proc.per.x = 1 
proc.per.y = 1 

# named random number distributions
distribution.move = int_uniform, 0, 2


